import { SquarefeetPipe } from './squarefeet.pipe';

describe('SquarefeetPipe', () => {
  it('create an instance', () => {
    const pipe = new SquarefeetPipe();
    expect(pipe).toBeTruthy();
  });
});
